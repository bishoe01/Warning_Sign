# Phase 8 구현 계획: 분석 결과 저장·재열람 (+ 멀티페이지)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 여러 장 계약서를 한 번에 분석하고, 결과를 기기 로컬에 저장해 목록에서 다시 보고 삭제한다.

**Architecture:** 촬영(N장) → 멀티 업로드 → 서버가 장별 OCR 후 텍스트를 합쳐 1회 분석 → 결과+이미지를 기기에 저장(AsyncStorage=기록 JSON, expo-file-system=이미지 파일) → 홈의 "내 기록"에서 재열람·삭제. 영속화는 `historyStore` 한 모듈에 격리. 서버는 OCR/AI 프록시 역할만(계약 데이터 서버 저장 X).

**Tech Stack:** React Native + Expo SDK 56, expo-router, AsyncStorage, expo-file-system(신규 File/Directory/Paths API, 동기), expo-image, react-native-gesture-handler(Swipeable), FastAPI(Python 3.9).

**설계 근거:** `03_Working/2026-06-01_phase8_저장재열람_spec.md` (v3)

> **검증 방식(중요):** 이 프로젝트는 자동 테스트 러너를 두지 않는다(spec §15, 사용자 결정). 따라서 본 계획은 기본 TDD 대신 **`tsc` 타입체크 + 서버 `curl` 스모크 + 시뮬레이터 수동 확인 + 잦은 커밋**으로 검증한다. 각 작업의 검증 단계는 실제 실행 가능한 명령/체크리스트로 명시한다.

---

## 파일 구조 (무엇을 어디서 책임지나)

### 앱 (`04_App/mobile`)
| 파일 | 신규/수정 | 책임 |
|---|---|---|
| `src/data/historyStore.ts` | 신규 | 저장/조회/삭제 + 이미지 파일 입출력의 **유일한 경계** |
| `src/data/session.ts` | 수정 | 분석 중 이미지(배열)·결과 임시 전달 |
| `src/api/contractApi.ts` | 수정 | N장 멀티파트 업로드 |
| `src/app/select.tsx` | 수정 | 멀티페이지 캡처(스트립·추가·삭제) |
| `src/app/loading.tsx` | 수정 | 멀티 업로드 + 저장 훅 + 멀티 로딩 문구 |
| `src/app/result.tsx` | 수정 | 새 분석/저장 기록 분기, 계약서 이미지 보기, 이동/저장 안내/삭제 |
| `src/app/history.tsx` | 신규 | 내역 목록 |
| `src/app/index.tsx` | 수정 | 홈 "내 기록" 진입점 |
| `src/app/_layout.tsx` | 수정 | `history` 라우트 등록 |

### 서버 (`04_App/server`)
| 파일 | 신규/수정 | 책임 |
|---|---|---|
| `main.py` | 수정 | `/analyze-contract` 가 `List[UploadFile]` 받아 장별 OCR 후 합쳐 분석 |
| `ocr_service.py`, `ai_service.py`, `models.py` | 변경 없음 | 페이지당 OCR/텍스트 입력은 동일 |

**구현 순서:** ① 의존성 → ② 서버 멀티 → ③ 전송 → ④ 캡처 → ⑤ 로딩 → ⑥ historyStore → ⑦ 저장 훅 → ⑧ 라우트 → ⑨ 목록 → ⑩ 홈 진입점 → ⑪ 상세 재열람.

---

## Task 1: 의존성 추가

**Files:** `04_App/mobile/package.json` (expo install 이 수정)

- [ ] **Step 1: 패키지 설치**

Run:
```bash
cd 04_App/mobile && npx expo install @react-native-async-storage/async-storage expo-file-system
```
Expected: 두 패키지가 SDK 56 호환 버전으로 `package.json` dependencies 에 추가.

- [ ] **Step 2: 타입체크가 깨지지 않는지 확인**

Run: `cd 04_App/mobile && npx tsc --noEmit`
Expected: 에러 없음(설치 전과 동일).

- [ ] **Step 3: 커밋**
```bash
git add 04_App/mobile/package.json 04_App/mobile/package-lock.json
git commit -m "chore(phase8): add async-storage + expo-file-system"
```

---

## Task 2: 서버 멀티페이지 (`main.py`)

**Files:** Modify `04_App/server/main.py`

- [ ] **Step 1: 엔드포인트를 다중 파일로 변경**

`main.py` 상단 import 에 `from typing import List` 를 추가하고, `analyze_contract` 를 아래로 교체한다:

```python
from typing import List
from fastapi import FastAPI, File, Form, UploadFile
# (기존 import 들 유지)

@app.post("/analyze-contract", response_model=AnalysisResult)
async def analyze_contract(
    files: List[UploadFile] = File(...),
    language: str = Form("ko"),
):
    if language not in ("ko", "en", "vi"):
        language = "ko"

    texts = []
    total_bytes = 0
    for f in files:
        data = await f.read()
        total_bytes += len(data)
        texts.append(run_ocr(data))
    ocr_text = "\n\n".join(t for t in texts if t)  # 페이지 구분으로 합치기
    result = analyze(ocr_text, language)

    # 안전: 계약서 이미지/원문은 저장·로그 X (페이지 수/총 바이트/모드만)
    print(
        f"[analyze] lang={language} pages={len(files)} bytes={total_bytes} "
        f"ocr={'real' if config.USE_REAL_OCR else 'sample'} "
        f"ai={'real' if config.USE_REAL_AI else 'sample'}"
    )
    return result
```

- [ ] **Step 2: 서버 실행**

Run: `cd 04_App/server && .venv/bin/uvicorn main:app --port 8000 &`
Expected: 起動 로그, `/health` 응답 가능.

- [ ] **Step 3: 단일/다중 파일 curl 스모크**

먼저 테스트 이미지 2장을 준비(아무 jpg 2개; 없으면 `assets` 의 png 도 가능):
```bash
curl -s -X POST localhost:8000/analyze-contract \
  -F "language=ko" \
  -F "files=@/path/to/page1.jpg" \
  -F "files=@/path/to/page2.jpg" | head -c 300
```
Expected: 200, JSON(`summary`/`cautionItems`/`notice` 포함). 키 없으면 샘플 JSON(정상). 서버 콘솔에 `pages=2` 로그.

- [ ] **Step 4: 서버 종료 + 커밋**
```bash
kill %1 2>/dev/null
git add 04_App/server/main.py
git commit -m "feat(phase8): accept multiple pages in /analyze-contract"
```

---

## Task 3: 전송 계층 (`session.ts`, `contractApi.ts`)

**Files:** Modify `04_App/mobile/src/data/session.ts`, `04_App/mobile/src/api/contractApi.ts`

- [ ] **Step 1: session 을 이미지 배열로**

`session.ts` 전체를 교체:
```ts
// 화면 간 임시 전달용(영속화 아님 — 그건 historyStore).
import { sampleAnalysis, type AnalysisResult } from '@/data/sampleAnalysis';

let pendingImages: string[] = [];
let lastResult: AnalysisResult = sampleAnalysis;

export const session = {
  setImages(uris: string[]) { pendingImages = uris; },
  getImages(): string[] { return pendingImages; },
  setResult(result: AnalysisResult) { lastResult = result; },
  getResult(): AnalysisResult { return lastResult; },
};
```

- [ ] **Step 2: contractApi 를 멀티파트로**

`contractApi.ts` 의 `analyzeContract` 를 교체:
```ts
export async function analyzeContract(
  imageUris: string[],
  language: Language,
): Promise<AnalysisResult> {
  const form = new FormData();
  form.append('language', language);
  imageUris.forEach((uri, i) => {
    form.append('files', {
      uri,
      name: `page-${i + 1}.jpg`,
      type: 'image/jpeg',
    } as unknown as Blob);
  });

  const res = await fetch(`${API_BASE_URL}/analyze-contract`, {
    method: 'POST',
    headers: { Accept: 'application/json' },
    body: form,
  });
  if (!res.ok) throw new Error(`server responded ${res.status}`);
  return (await res.json()) as AnalysisResult;
}
```
(import 줄 `import type { AnalysisResult, Language }` 은 그대로 유지)

- [ ] **Step 3: 호출부 최소 패치(그린 유지)**

전송 시그니처가 바뀌었으니 호출 두 곳을 최소 수정한다(전체 멀티페이지 UI 는 Task 4·5 에서 완성). 이렇게 해야 이 커밋이 타입체크를 통과한다.

`select.tsx` 의 `analyze` 안:
```ts
session.setImage(photoUri);   // → 변경
session.setImages([photoUri]);
```
`loading.tsx` 의 분석 IIFE 앞부분:
```ts
const uri = session.getImage();   // → const uris = session.getImages();
// ...
if (uri) {                        // → if (uris.length > 0) {
  result = await analyzeContract(uri, 'ko');   // → analyzeContract(uris, 'ko')
}
```

- [ ] **Step 4: 타입체크(그린)**

Run: `cd 04_App/mobile && npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 5: 커밋**
```bash
git add 04_App/mobile/src/data/session.ts 04_App/mobile/src/api/contractApi.ts 04_App/mobile/src/app/select.tsx 04_App/mobile/src/app/loading.tsx
git commit -m "feat(phase8): multi-image transport (session + contractApi)"
```

---

## Task 4: 멀티페이지 캡처 (`select.tsx`)

**Files:** Modify `04_App/mobile/src/app/select.tsx`

핵심 변경: 단일 `photoUri` → `pages: string[]`. 촬영/갤러리로 추가, 썸네일 스트립(가로)에서 페이지 번호 + 삭제(×) + 탭하여 크게 보기, "분석하기 (N장)".

- [ ] **Step 1: 상태·핸들러 교체**

`const [photoUri, setPhotoUri] = useState<string | null>(null);` 를 아래로 교체:
```ts
const [pages, setPages] = useState<string[]>([]);
const [preview, setPreview] = useState<string | null>(null); // 크게 보기 모달
```
`takePhoto` / `pickFromGallery` / `analyze` 를 교체:
```ts
const takePhoto = async () => {
  if (busy) return;
  setBusy(true);
  try {
    const photo = await cameraRef.current?.takePictureAsync({ quality: 0.9 });
    if (photo?.uri) setPages((p) => [...p, photo.uri]);
  } finally {
    setBusy(false);
  }
};

const pickFromGallery = async () => {
  const perm = await ImagePicker.requestMediaLibraryPermissionsAsync();
  if (!perm.granted) return;
  const result = await ImagePicker.launchImageLibraryAsync({
    mediaTypes: ['images'],
    allowsMultipleSelection: true,
    quality: 1,
  });
  if (!result.canceled) setPages((p) => [...p, ...result.assets.map((a) => a.uri)]);
};

const removePage = (i: number) => setPages((p) => p.filter((_, idx) => idx !== i));

const analyze = () => {
  if (pages.length === 0) return;
  session.setImages(pages);
  router.push('/loading');
};
```

- [ ] **Step 2: 권한 거부 화면 가드 수정**

기존 `if (!permission.granted && !photoUri)` → `if (!permission.granted && pages.length === 0)`.

- [ ] **Step 3: 카메라는 항상 라이브 + 하단에 스트립/컨트롤**

라이브 미리보기는 항상 카메라(촬영 후에도 다음 장 찍게). `frame` 내부의 `photoUri ? <Image/> : <CameraView/>` 조건을 제거하고 **항상 `CameraView`** 를 렌더한다(스캔라인 포함). 힌트 문구를 페이지 수 기반으로:
```tsx
<Text style={styles.hint}>
  {pages.length === 0
    ? '계약서를 네모 칸에 꽉 차게 맞춰 주세요'
    : `${pages.length}장 추가됨 · 다음 장을 찍거나 분석을 시작하세요`}
</Text>
```

**남은 `photoUri` 참조 2곳도 정리**(안 하면 `photoUri is not defined` tsc 에러):
- Header 호출: `onTorch={photoUri ? undefined : () => setTorch((v) => !v)}` → `onTorch={() => setTorch((v) => !v)}` (항상 라이브라 토치 상시 가능)
- 하단 팁: `{!photoUri && (<Text style={[styles.tip, …]}>그림자 없이…</Text>)}` → `{pages.length === 0 && ( … )}` (촬영 가이드는 첫 장 전에만 노출)

- [ ] **Step 4: 컨트롤 영역 — 셔터 + 갤러리 + 썸네일 스트립 + 분석 버튼**

기존 `controls` 의 `photoUri ? 확인행 : 셔터행` 구조를 아래로 교체(셔터·갤러리는 항상, 스트립과 분석 버튼은 1장 이상일 때):
```tsx
<View style={[styles.controls, { paddingBottom: insets.bottom + spacing.sm }]}>
  {pages.length > 0 && (
    <ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.strip}
      contentContainerStyle={styles.stripContent}>
      {pages.map((uri, i) => (
        <Pressable key={uri + i} style={styles.thumbWrap} onPress={() => setPreview(uri)}>
          <Image source={{ uri }} style={styles.thumb} resizeMode="cover" />
          <Text style={styles.thumbNum}>{i + 1}</Text>
          <Pressable hitSlop={8} style={styles.thumbDel} onPress={() => removePage(i)}>
            <Feather name="x" size={12} color={colors.white} />
          </Pressable>
        </Pressable>
      ))}
    </ScrollView>
  )}

  <View style={styles.shutterRow}>
    <Pressable style={({ pressed }) => [styles.sideButton, pressed && styles.pressed]}
      onPress={pickFromGallery}>
      <Feather name="image" size={22} color={colors.cameraTextDim} />
    </Pressable>
    <Pressable onPress={takePhoto} disabled={busy} hitSlop={12}>
      {({ pressed }) => (
        <View style={[styles.shutterOuter, pressed && { transform: [{ scale: 0.93 }] }]}>
          <View style={styles.shutterInner} />
        </View>
      )}
    </Pressable>
    <View style={styles.sideButton} />
  </View>

  {pages.length > 0 && (
    <Pressable style={({ pressed }) => [styles.analyzeButton, shadow.button, pressed && styles.pressed]}
      onPress={analyze}>
      <Text style={styles.analyzeText}>분석하기 ({pages.length}장)</Text>
    </Pressable>
  )}
  <Text style={styles.tip}>썸네일을 탭하면 크게 볼 수 있고, ×로 지울 수 있어요</Text>
</View>
```

- [ ] **Step 5: 크게 보기 모달 추가**

컴포넌트 return 의 최상위 `<View>` 안 끝부분에 추가:
```tsx
<Modal visible={!!preview} transparent animationType="fade" onRequestClose={() => setPreview(null)}>
  <Pressable style={styles.previewBackdrop} onPress={() => setPreview(null)}>
    {preview && <Image source={{ uri: preview }} style={styles.previewImg} resizeMode="contain" />}
  </Pressable>
</Modal>
```
import 에 `Modal`, `ScrollView` 추가(`react-native`).

- [ ] **Step 6: 새 스타일 추가**

`StyleSheet.create` 에 추가:
```ts
strip: { maxHeight: 76, marginBottom: spacing.md },
stripContent: { gap: spacing.sm, paddingHorizontal: 2 },
thumbWrap: { width: 54, height: 70, borderRadius: 8, overflow: 'hidden', backgroundColor: colors.cameraSurface },
thumb: { width: '100%', height: '100%' },
thumbNum: { position: 'absolute', left: 3, bottom: 3, fontSize: 10, fontWeight: '800', color: colors.white,
  backgroundColor: 'rgba(0,0,0,0.5)', paddingHorizontal: 4, borderRadius: 4, overflow: 'hidden' },
thumbDel: { position: 'absolute', top: 2, right: 2, width: 16, height: 16, borderRadius: 8,
  backgroundColor: 'rgba(0,0,0,0.6)', alignItems: 'center', justifyContent: 'center' },
previewBackdrop: { flex: 1, backgroundColor: 'rgba(0,0,0,0.92)', alignItems: 'center', justifyContent: 'center', padding: spacing.lg },
previewImg: { width: '100%', height: '80%' },
```
(기존 `confirmRow`/`retakeButton`/`retakeText` 스타일은 더 이상 안 쓰이면 남겨둬도 무방 — 본인 변경으로 생긴 미사용만 정리)

- [ ] **Step 7: 타입체크 + 수동 확인**

Run: `cd 04_App/mobile && npx tsc --noEmit` → 에러 없음.
수동(시뮬레이터/Expo Go): 카메라 화면에서 2장 촬영 → 스트립에 1,2 표시 → 썸네일 탭 시 크게 보기 → ×로 1장 삭제 → "분석하기 (1장)" → loading 이동.

- [ ] **Step 8: 커밋**
```bash
git add 04_App/mobile/src/app/select.tsx
git commit -m "feat(phase8): multi-page capture with thumbnail strip"
```

---

## Task 5: 로딩 — 멀티 업로드 + 문구 (`loading.tsx`)

**Files:** Modify `04_App/mobile/src/app/loading.tsx`

- [ ] **Step 1: 이미지 배열 사용 + 폴백 플래그**

분석 IIFE 를 교체(저장 훅은 Task 7 에서 추가):
```ts
void (async () => {
  const uris = session.getImages();
  let result = sampleAnalysis;
  let isSample = uris.length === 0; // 이미지 없으면 샘플
  try {
    if (uris.length > 0) {
      result = await analyzeContract(uris, 'ko');
    }
  } catch {
    result = sampleAnalysis;
    isSample = true; // 서버 실패 → 샘플 폴백
  }
  if (cancelled) return;
  session.setResult(result);
  // (Task 7) historyStore.save(result, uris, { isSample });
  const wait = Math.max(0, minDisplayMs - (Date.now() - startedAt));
  timers.push(setTimeout(() => { if (!cancelled) router.replace('/result'); }, wait));
})();
```

- [ ] **Step 2: 멀티페이지 인지 문구 + 마지막 단계가 "멈춘 척" 안 하게**

`STEPS` 의 OCR 단계 문구를 페이지 수 인지형으로. 컴포넌트 안에서 페이지 수를 읽어 동적 문구를 만든다:
```ts
const pageCount = session.getImages().length;
const STEPS = [
  '계약서 이미지를 업로드하고 있어요.',
  pageCount > 1 ? `${pageCount}장을 읽고 있어요. 장수가 많으면 조금 더 걸려요.` : 'OCR로 계약서 문장을 읽고 있어요.',
  'AI가 임금·근무시간·휴일 조건을 분석하고 있어요.',
  '확인이 필요한 조항을 쉬운 문장으로 정리하고 있어요.',
];
```
(STEPS 가 모듈 상수였다면 컴포넌트 내부 `const` 로 옮긴다.) 마지막 단계 도달 후에도 펄스 링 애니메이션은 계속 도므로 "멈춤"처럼 보이지 않는다 — 추가 작업 불필요. 단, 실제 분석이 끝나야 이동하므로 문구만 보강.

- [ ] **Step 3: 타입체크 + 수동 확인**

Run: `cd 04_App/mobile && npx tsc --noEmit` → 에러 없음.
수동: 서버 켠 상태에서 2장 분석 → "2장을 읽고 있어요…" 표시 → 결과 화면 도착.

- [ ] **Step 4: 커밋**
```bash
git add 04_App/mobile/src/app/loading.tsx
git commit -m "feat(phase8): multi-image analyze + page-aware loading copy"
```

---

## Task 6: 로컬 저장소 (`historyStore.ts`)

**Files:** Create `04_App/mobile/src/data/historyStore.ts`

> 이미지 경로는 **절대 uri 를 저장하지 않는다**(iOS 컨테이너 경로 변경 시 깨짐). 파일명만 저장하고 읽을 때 `Paths.document` 기준으로 복원한다.

- [ ] **Step 1: 모듈 작성**
```ts
import AsyncStorage from '@react-native-async-storage/async-storage';
import { Directory, File, Paths } from 'expo-file-system';

import type { AnalysisResult } from '@/data/sampleAnalysis';

const KEY = '@contract-helper/history';
const ROOT = 'history'; // documentDirectory 하위 폴더

export type HistoryRecord = {
  id: string;            // createdAt 문자열
  createdAt: number;     // epoch ms
  result: AnalysisResult;
  imageFiles: string[];  // 기록 폴더 내 파일명 (예: ['page-1.jpg'])
  isSample?: boolean;
};

async function readAll(): Promise<HistoryRecord[]> {
  try {
    const raw = await AsyncStorage.getItem(KEY);
    if (!raw) return [];
    const parsed = JSON.parse(raw);
    return Array.isArray(parsed) ? parsed : [];
  } catch {
    return [];
  }
}

async function writeAll(records: HistoryRecord[]): Promise<void> {
  await AsyncStorage.setItem(KEY, JSON.stringify(records));
}

// 기록의 페이지 이미지를 현재 기기 기준 절대 uri 로 복원
export function pageUris(record: HistoryRecord): string[] {
  return record.imageFiles.map((name) => new File(Paths.document, ROOT, record.id, name).uri);
}

export async function save(
  result: AnalysisResult,
  imageUris: string[],
  opts: { isSample?: boolean } = {},
): Promise<HistoryRecord> {
  const createdAt = Date.now();
  const id = String(createdAt);
  const imageFiles: string[] = [];
  if (imageUris.length > 0) {
    const dir = new Directory(Paths.document, ROOT, id);
    dir.create({ intermediates: true });
    imageUris.forEach((uri, i) => {
      const name = `page-${i + 1}.jpg`;
      new File(uri).copy(new File(dir, name));
      imageFiles.push(name);
    });
  }
  const record: HistoryRecord = { id, createdAt, result, imageFiles, isSample: opts.isSample };
  const all = await readAll();
  await writeAll([record, ...all]);
  return record;
}

export async function list(): Promise<HistoryRecord[]> {
  const all = await readAll();
  return all.sort((a, b) => b.createdAt - a.createdAt);
}

export async function get(id: string): Promise<HistoryRecord | null> {
  const all = await readAll();
  return all.find((r) => r.id === id) ?? null;
}

export async function remove(id: string): Promise<void> {
  const all = await readAll();
  await writeAll(all.filter((r) => r.id !== id));
  try { new Directory(Paths.document, ROOT, id).delete(); } catch { /* 없으면 무시 */ }
}

export async function clearAll(): Promise<void> {
  await AsyncStorage.removeItem(KEY);
  try { new Directory(Paths.document, ROOT).delete(); } catch { /* 무시 */ }
}
```

- [ ] **Step 2: 타입체크**

Run: `cd 04_App/mobile && npx tsc --noEmit`
Expected: 에러 없음. (만약 `Directory.create` 옵션 타입이 다르면 expo-file-system SDK 56 docs 확인 — 동작은 폴더 생성으로 동일.)

- [ ] **Step 3: 커밋**
```bash
git add 04_App/mobile/src/data/historyStore.ts
git commit -m "feat(phase8): historyStore (AsyncStorage + file images, relative paths)"
```

---

## Task 7: 저장 훅 (`loading.tsx`)

**Files:** Modify `04_App/mobile/src/app/loading.tsx`

- [ ] **Step 1: import 추가**
```ts
import * as historyStore from '@/data/historyStore';
```

- [ ] **Step 2: 결과 확정 후 저장(best-effort)**

Task 5 의 주석 자리 `// (Task 7) ...` 를 실제 호출로:
```ts
session.setResult(result);
void historyStore.save(result, uris, { isSample }).catch(() => { /* 저장 실패는 흐름 안 막음 */ });
```

- [ ] **Step 3: 타입체크 + 수동 확인(영속성)**

Run: `cd 04_App/mobile && npx tsc --noEmit` → 에러 없음.
수동: 분석 1회 → (다음 Task 의 목록에서 보일 예정). 지금은 에러 없이 결과까지 도달하면 OK.

- [ ] **Step 4: 커밋**
```bash
git add 04_App/mobile/src/app/loading.tsx
git commit -m "feat(phase8): persist analysis to historyStore after analyze"
```

---

## Task 8: 라우트 등록 (`_layout.tsx`)

**Files:** Modify `04_App/mobile/src/app/_layout.tsx`

> ⚠️ **순서 주의:** 이 등록은 **Task 9(history.tsx 생성) 이후**에 한다 — 파일 없는 라우트를 선언하면 expo-router 가 "No route named history" 경고/에러를 낼 수 있다. 사실 파일 기반 라우팅이 `history.tsx` 를 자동 포함하므로 **생략해도 동작**한다(기존 화면들과 명시적 일관성을 위해서만 추가). 생략하면 이 Task 자체를 건너뛴다.

- [ ] **Step 1: history 스크린 추가**

`<Stack.Screen name="result" />` 다음 줄에 추가:
```tsx
<Stack.Screen name="history" />
```

- [ ] **Step 2: 타입체크 + 커밋**
```bash
cd 04_App/mobile && npx tsc --noEmit
git add 04_App/mobile/src/app/_layout.tsx
git commit -m "feat(phase8): register history route"
```

---

## Task 9: 내역 목록 (`history.tsx`)

**Files:** Create `04_App/mobile/src/app/history.tsx`

행: 썸네일(1페이지, 없으면 플레이스홀더) + "N장" 배지 + 절대 날짜 + 요약(계약기간·월급) + 주의 N건 + 샘플 배지. 스와이프 삭제 + 헤더 전체삭제 + "이 기기에만 저장" 안내 + 빈 상태.

- [ ] **Step 1: 화면 작성**
```tsx
import { Feather } from '@expo/vector-icons';
import { Image } from 'expo-image';
import { useFocusEffect, useRouter } from 'expo-router';
import { useCallback, useState } from 'react';
import { Alert, Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { Swipeable } from 'react-native-gesture-handler';
import { SafeAreaView } from 'react-native-safe-area-context';

import { colors, radius, shadow, spacing } from '@/constants/theme';
import * as historyStore from '@/data/historyStore';
import type { HistoryRecord } from '@/data/historyStore';

function formatDate(ms: number): string {
  const d = new Date(ms);
  const p = (n: number) => String(n).padStart(2, '0');
  return `${d.getFullYear()}.${p(d.getMonth() + 1)}.${p(d.getDate())} ${p(d.getHours())}:${p(d.getMinutes())}`;
}

export default function HistoryScreen() {
  const router = useRouter();
  const [records, setRecords] = useState<HistoryRecord[]>([]);

  const reload = useCallback(() => { historyStore.list().then(setRecords); }, []);
  useFocusEffect(useCallback(() => { reload(); }, [reload]));

  const onDelete = (id: string) => { historyStore.remove(id).then(reload); };
  const onClearAll = () => {
    if (records.length === 0) return;
    Alert.alert('전체 삭제', '저장된 분석 기록을 모두 지울까요?', [
      { text: '취소', style: 'cancel' },
      { text: '전체 삭제', style: 'destructive', onPress: () => historyStore.clearAll().then(reload) },
    ]);
  };

  return (
    <SafeAreaView style={styles.safe} edges={['top']}>
      <View style={styles.header}>
        <Pressable onPress={() => router.back()} hitSlop={10} style={styles.backBtn}>
          <Feather name="chevron-left" size={26} color={colors.text} />
        </Pressable>
        <Text style={styles.headerTitle}>내 분석 기록</Text>
        <Pressable onPress={onClearAll} hitSlop={10} style={styles.backBtn}>
          {records.length > 0 && <Feather name="trash-2" size={20} color={colors.textTertiary} />}
        </Pressable>
      </View>

      {records.length === 0 ? (
        <View style={styles.empty}>
          <Feather name="inbox" size={40} color={colors.border} />
          <Text style={styles.emptyText}>아직 저장된 분석이 없어요</Text>
          <Pressable style={styles.emptyCta} onPress={() => router.replace('/select')}>
            <Text style={styles.emptyCtaText}>계약서 분석 시작</Text>
          </Pressable>
        </View>
      ) : (
        <ScrollView contentContainerStyle={styles.list}>
          <Text style={styles.privacyNote}>이 기록은 이 기기에만 저장돼요</Text>
          {records.map((r) => {
            const uris = historyStore.pageUris(r);
            return (
              <Swipeable key={r.id} renderRightActions={() => (
                <Pressable style={styles.swipeDelete} onPress={() => onDelete(r.id)}>
                  <Feather name="trash-2" size={20} color={colors.white} />
                  <Text style={styles.swipeDeleteText}>삭제</Text>
                </Pressable>
              )}>
                <Pressable style={styles.row} onPress={() => router.push(`/result?id=${r.id}`)}>
                  <View style={styles.thumbBox}>
                    {uris[0]
                      ? <Image source={{ uri: uris[0] }} style={styles.thumb} contentFit="cover" />
                      : <Feather name="file-text" size={22} color={colors.textTertiary} />}
                    {r.imageFiles.length > 1 && <Text style={styles.pageBadge}>{r.imageFiles.length}장</Text>}
                  </View>
                  <View style={styles.rowBody}>
                    <View style={styles.rowTop}>
                      <Text style={styles.rowDate}>{formatDate(r.createdAt)}</Text>
                      {r.isSample && <Text style={styles.sampleBadge}>샘플</Text>}
                    </View>
                    <Text style={styles.rowSummary} numberOfLines={1}>
                      {r.result.summary.contractPeriod} · {r.result.summary.salary}
                    </Text>
                    <Text style={styles.rowCaution}>주의 {r.result.cautionItems.length}건</Text>
                  </View>
                  <Feather name="chevron-right" size={20} color={colors.textTertiary} />
                </Pressable>
              </Swipeable>
            );
          })}
        </ScrollView>
      )}
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safe: { flex: 1, backgroundColor: colors.surface },
  header: { flexDirection: 'row', alignItems: 'center', paddingHorizontal: spacing.md, paddingVertical: spacing.sm },
  backBtn: { width: 34, height: 34, alignItems: 'center', justifyContent: 'center' },
  headerTitle: { flex: 1, fontSize: 16, fontWeight: '700', color: colors.text, textAlign: 'center' },
  empty: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: spacing.md },
  emptyText: { fontSize: 15, color: colors.textSecondary, fontWeight: '600' },
  emptyCta: { marginTop: spacing.sm, backgroundColor: colors.primary, paddingVertical: 13, paddingHorizontal: spacing.xxl, borderRadius: radius.lg },
  emptyCtaText: { color: colors.white, fontSize: 15, fontWeight: '800' },
  list: { padding: spacing.lg, gap: spacing.md },
  privacyNote: { fontSize: 12, color: colors.textTertiary, textAlign: 'center', marginBottom: spacing.xs },
  row: { flexDirection: 'row', alignItems: 'center', gap: spacing.md, backgroundColor: colors.surface,
    borderRadius: radius.lg, padding: spacing.md, ...shadow.card },
  thumbBox: { width: 52, height: 64, borderRadius: 8, backgroundColor: colors.bgElevated, alignItems: 'center', justifyContent: 'center', overflow: 'hidden' },
  thumb: { width: '100%', height: '100%' },
  pageBadge: { position: 'absolute', right: 2, bottom: 2, fontSize: 9, fontWeight: '800', color: colors.white,
    backgroundColor: 'rgba(0,0,0,0.6)', paddingHorizontal: 4, paddingVertical: 1, borderRadius: 4, overflow: 'hidden' },
  rowBody: { flex: 1, gap: 3 },
  rowTop: { flexDirection: 'row', alignItems: 'center', gap: 6 },
  rowDate: { fontSize: 12, color: colors.textTertiary, fontWeight: '600' },
  sampleBadge: { fontSize: 10, fontWeight: '800', color: colors.textSecondary, backgroundColor: colors.bgElevated,
    paddingHorizontal: 6, paddingVertical: 1, borderRadius: 5, overflow: 'hidden' },
  rowSummary: { fontSize: 14, color: colors.text, fontWeight: '700' },
  rowCaution: { fontSize: 12, color: colors.primary, fontWeight: '700' },
  swipeDelete: { width: 88, backgroundColor: '#E5484D', alignItems: 'center', justifyContent: 'center', gap: 3, borderRadius: radius.lg, marginLeft: spacing.sm },
  swipeDeleteText: { color: colors.white, fontSize: 12, fontWeight: '700' },
});
```

> 삭제 배경은 테마에 `danger` 색이 없어 리터럴 `'#E5484D'` 사용(타입 안전). 원하면 `theme.ts` 에 `danger: '#E5484D'` 추가 후 `colors.danger` 로 교체.

- [ ] **Step 2: 타입체크 + 수동 확인**

Run: `cd 04_App/mobile && npx tsc --noEmit` → 에러 없음.
수동: (Task 10 후 진입점으로) 분석 2건 → 목록에 최신순, 썸네일·"N장"·날짜·요약·"주의 N건" → 스와이프 삭제 → 사라짐 → 전체삭제 → 빈 상태.

- [ ] **Step 3: 커밋**
```bash
git add 04_App/mobile/src/app/history.tsx
git commit -m "feat(phase8): history list screen (swipe delete, clear all, empty state)"
```

---

## Task 10: 홈 진입점 (`index.tsx`)

**Files:** Modify `04_App/mobile/src/app/index.tsx`

- [ ] **Step 1: 브랜드 행 오른쪽에 "내 기록" 진입점**

`brandRow` `<View>` 를 아이콘+라벨 진입점을 포함하도록 수정. 기존:
```tsx
<View style={styles.brandRow}>
  <View style={styles.brandMark}><Feather name="file-text" size={15} color={colors.white} /></View>
  <Text style={styles.brandName}>근로계약 도우미</Text>
</View>
```
→ 교체:
```tsx
<View style={styles.brandRow}>
  <View style={styles.brandMark}><Feather name="file-text" size={15} color={colors.white} /></View>
  <Text style={styles.brandName}>근로계약 도우미</Text>
  <View style={{ flex: 1 }} />
  <Pressable style={styles.historyEntry} hitSlop={8} onPress={() => router.push('/history')}>
    <Feather name="clock" size={15} color={colors.textSecondary} />
    <Text style={styles.historyEntryText}>내 기록</Text>
  </Pressable>
</View>
```

- [ ] **Step 2: 스타일 추가**
```ts
historyEntry: { flexDirection: 'row', alignItems: 'center', gap: 4, paddingVertical: 5, paddingHorizontal: 10,
  backgroundColor: colors.bgElevated, borderRadius: 999 },
historyEntryText: { fontSize: 12, fontWeight: '700', color: colors.textSecondary },
```

- [ ] **Step 3: 타입체크 + 수동 확인 + 커밋**
```bash
cd 04_App/mobile && npx tsc --noEmit
```
수동: 홈 우상단 "내 기록" → 목록 이동.
```bash
git add 04_App/mobile/src/app/index.tsx
git commit -m "feat(phase8): home entry point to history"
```

---

## Task 11: 상세 재열람 + 이미지 + 이동/저장 안내/삭제 (`result.tsx`)

**Files:** Modify `04_App/mobile/src/app/result.tsx`

- [ ] **Step 1: import + 데이터 분기 + 로딩 가드**

import 추가/수정:
```ts
import { Image } from 'expo-image';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { useEffect, useState } from 'react';
import { ActivityIndicator, Alert, Modal } from 'react-native'; // 기존 react-native import 에 추가
import * as historyStore from '@/data/historyStore';
import { session } from '@/data/session';
```
컴포넌트 상단 — **모든 훅을 먼저 선언한 뒤** 가드를 둔다(rules-of-hooks 위반 금지). 기존 `const [language, setLanguage] = useState<Language>('ko')` 다음에 이어서:
```ts
const { id } = useLocalSearchParams<{ id?: string }>();
const fromHistory = !!id;
const [record, setRecord] = useState<historyStore.HistoryRecord | null>(null);
const [viewer, setViewer] = useState<string | null>(null);
useEffect(() => { if (id) historyStore.get(id).then(setRecord); }, [id]);

// 기록 로딩 중엔 직전 분석(session)이 한 프레임 보이는 깜빡임 방지 (모든 훅 선언 뒤에 위치)
if (fromHistory && !record) {
  return (
    <SafeAreaView style={styles.safe} edges={['top']}>
      <View style={styles.header}>
        <Pressable onPress={() => router.back()} hitSlop={10} style={styles.backBtn}>
          <Feather name="chevron-left" size={26} color={colors.text} />
        </Pressable>
        <Text style={styles.headerTitle}>분석 결과</Text>
        <View style={styles.backBtn} />
      </View>
      <View style={styles.center}><ActivityIndicator color={colors.primary} /></View>
    </SafeAreaView>
  );
}

const data = fromHistory ? record!.result : session.getResult();
const imageUris = fromHistory ? historyStore.pageUris(record!) : session.getImages();
const t = labels[language]; // 기존 줄 유지 (가드 다음)
```

- [ ] **Step 2: 헤더 — 맥락별 이동 + 저장 기록 삭제**

헤더의 뒤로가기 동작과 우측 액션을 교체:
```tsx
<View style={styles.header}>
  <Pressable onPress={() => fromHistory ? router.back() : router.replace('/')} hitSlop={10} style={styles.backBtn}>
    <Feather name={fromHistory ? 'chevron-left' : 'home'} size={fromHistory ? 26 : 22} color={colors.text} />
  </Pressable>
  <Text style={styles.headerTitle}>분석 결과</Text>
  {fromHistory ? (
    <Pressable hitSlop={10} style={styles.backBtn} onPress={() => {
      Alert.alert('삭제', '이 분석 기록을 지울까요?', [
        { text: '취소', style: 'cancel' },
        { text: '삭제', style: 'destructive', onPress: () => historyStore.remove(id!).then(() => router.back()) },
      ]);
    }}>
      <Feather name="trash-2" size={20} color={colors.textTertiary} />
    </Pressable>
  ) : <View style={styles.backBtn} />}
</View>
```

- [ ] **Step 3: 새 분석이면 "저장됨" 안내**

`<LanguageTabs .../>` 바로 위(ScrollView content 최상단)에 추가:
```tsx
{!fromHistory && (
  <View style={styles.savedNote}>
    <Feather name="check-circle" size={14} color={colors.primary} />
    <Text style={styles.savedNoteText}>이 기기에 저장됐어요 · 홈의 '내 기록'에서 다시 볼 수 있어요</Text>
  </View>
)}
```

- [ ] **Step 4: 계약서 원본 이미지 섹션(분석 아래 2차 노출)**

`notice` 블록 다음(ScrollView content 끝)에 추가:
```tsx
{imageUris.length > 0 && (
  <View style={styles.originalBlock}>
    <Text style={styles.sectionTitle}>계약서 원본 {imageUris.length}장</Text>
    <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ gap: spacing.sm }}>
      {imageUris.map((uri, i) => (
        <Pressable key={uri + i} onPress={() => setViewer(uri)}>
          <Image source={{ uri }} style={styles.originalThumb} contentFit="cover" />
        </Pressable>
      ))}
    </ScrollView>
  </View>
)}
```
그리고 SafeAreaView 끝에 전체화면 뷰어 추가:
```tsx
<Modal visible={!!viewer} transparent animationType="fade" onRequestClose={() => setViewer(null)}>
  <Pressable style={styles.viewerBackdrop} onPress={() => setViewer(null)}>
    {viewer && <Image source={{ uri: viewer }} style={styles.viewerImg} contentFit="contain" />}
  </Pressable>
</Modal>
```

- [ ] **Step 5: 스타일 추가**
```ts
savedNote: { flexDirection: 'row', alignItems: 'center', gap: 6, backgroundColor: colors.primarySoft,
  borderRadius: radius.md, paddingVertical: 9, paddingHorizontal: spacing.md },
savedNoteText: { flex: 1, fontSize: 12, color: colors.primary, fontWeight: '700' },
originalBlock: { marginTop: spacing.sm, gap: spacing.sm },
originalThumb: { width: 92, height: 124, borderRadius: radius.md, backgroundColor: colors.bgElevated },
viewerBackdrop: { flex: 1, backgroundColor: 'rgba(0,0,0,0.92)', alignItems: 'center', justifyContent: 'center', padding: spacing.lg },
viewerImg: { width: '100%', height: '85%' },
center: { flex: 1, alignItems: 'center', justifyContent: 'center' },
```

- [ ] **Step 6: 타입체크 + 수동 확인(끝까지 한 흐름)**

Run: `cd 04_App/mobile && npx tsc --noEmit` → 에러 없음.
수동 전체 흐름:
1. 홈 → 촬영 2장 → 분석 → 결과 상단 "저장됨" 안내, 하단 "계약서 원본 2장" → 썸네일 탭 전체화면.
2. 결과에서 홈 아이콘 → 홈 이동(카메라로 안 떨어짐).
3. 홈 "내 기록" → 목록에 방금 분석(썸네일·2장·날짜·요약) → 탭 → 상세(뒤로가기 = 목록), 휴지통 → 삭제 후 목록.
4. **앱 완전 재시작 → 목록 유지**(영속성).
5. 서버 끈 채 분석 → 샘플 폴백 → 목록에 "샘플" 배지.

- [ ] **Step 7: 커밋**
```bash
git add 04_App/mobile/src/app/result.tsx
git commit -m "feat(phase8): re-view saved record, contract image viewer, context nav, delete"
```

---

## 마무리 검증 (전체)

- [ ] `cd 04_App/mobile && npx tsc --noEmit` 통과
- [ ] iOS 또는 Android 번들 통과(예: `npx expo export --platform ios` 또는 시뮬레이터 실행)
- [ ] 위 Task 11 수동 흐름 1~5 전부 통과
- [ ] 서버 `pages=N` 로그로 멀티페이지 수신 확인

---

## Self-Review (spec 대비)

- **spec §7 캡처** → Task 4 ✓ / **§8 전송** → Task 3 ✓ / **§9 서버** → Task 2 ✓
- **§10 historyStore** → Task 6 ✓ (단, 견고성 위해 `imagePaths`(절대) 대신 `imageFiles`(파일명)+`pageUris()` 복원으로 구현 — iOS 컨테이너 경로 변경 대응)
- **§11 저장 흐름** → Task 5+7 ✓ / **§12 홈** → Task 10 ✓ / **목록** → Task 9 ✓ / **상세** → Task 11 ✓
- **§13 삭제** → Task 9(스와이프·전체) + Task 11(상세 휴지통) ✓
- **§14 의존성** → Task 1 ✓
- **§17 UX**: ①캡처확인=Task4 / ②로딩문구=Task5 / ③결과후이동=Task11 / ④저장안내+진입점=Task7·10·11 / ⑤삭제발견성=Task9·11 / ⑥이미지2차노출=Task11 / ⑦날짜·샘플배지·기기저장안내=Task9 ✓
- **타입 일관성**: `session.setImages/getImages`, `analyzeContract(uris[])`, `historyStore.save/list/get/remove/clearAll/pageUris`, `HistoryRecord.imageFiles` — 전 작업에서 동일 시그니처 사용 확인.
- **구현 중 확인할 점(🟡)**: ① `Directory.create({ intermediates })` 옵션명 + `new File(외부캐시uri).copy()` 동작(Task 6, 안 되면 expo-file-system SDK56 docs 대안). ② `Swipeable` import(클래식 vs `react-native-gesture-handler/ReanimatedSwipeable`, gesture-handler 2.31 deprecation) — Task 9. ③ Task 1 커밋의 lockfile 이름(npm `package-lock.json` / yarn / bun 확인 후 add).
