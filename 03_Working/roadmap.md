# Roadmap: AI 근로계약서 도우미 MVP

> 상태: DRAFT  
> 기준일: 2026-06-06
> 원칙: 화면 → 이미지 → 서버 → OCR → AI → 결과 → 제출물 순서로 붙인다.

> ⚠️ **상태 기준은 `progress.json`** 입니다. 아래 Phase 0~6 표의 개별 `todo` 표기는 낡았을 수 있으니, 실제 완료 여부는 progress.json 을 신뢰하세요.

## 현재 상태 & 다음 세션 시작점 (2026-06-06)

**이미 된 것:** 백엔드 OCR(Google Vision)+AI(OpenAI) end-to-end 동작. 앱 UI 를 Toss 톤으로 리디자인 + 인앱 카메라 스캐너 구현(타입체크·iOS 번들 통과, 홈/촬영/결과 화면 시뮬레이터 확인). 팀원 iPhone Expo Go 테스트를 위해 Expo SDK 54로 다운그레이드 완료(`expo-doctor` 18/18, `expo start --go` 시작 확인). → `2026-06-01_UI리디자인_결정.md`

**다음 세션은 여기서 시작:**

1. **Phase 8 (저장·재열람 + 멀티페이지)** — ✅ **구현 완료(2026-06-02).** plan Task 1~11 → 12 커밋(main, 미푸시). tsc + iOS 번들(`expo export`) 통과. 카메라 UX 개편(A: 촬영/검토 2모드+캐러셀+active 게이팅)도 구현 완료(tsc 통과). Expo SDK 54 전환 완료. **남은 건 App Store Expo Go 실기기 수동 테스트 + `git push`.** 실행: `cd 04_App/mobile && npx expo start --go`.
2. **Phase 9 (다국어 확장 + 마이페이지)** — ✅ **구현 완료(2026-06-02).** 언어 선택 토글/리스트, 마이페이지, 앱 전역 i18n, localized 분석 결과 모델, 서버 16개 언어 파라미터/프롬프트, 16개 언어 UI 문구 적용 완료. 실제 번역 문구는 원어민 검수 권장. 설계: `2026-06-02_phase9_IA_다국어_설계.md`, 완료 체크: `2026-06-02_phase9_구현계획.md`.
3. **다음 기획 순서 확정(2026-06-03)** — 구현을 바로 밀기보다 `2026-06-03_다음기획_프롬프트_UIUX_출처하이라이트_다국어.md` 기준으로 정리한다. 순서: 프롬프트 품질 조율 → 동적 다국어 보강 → 결과 UI/UX 정비 → OCR 좌표 기반 사진 출처 하이라이트 → 데모 QA.
4. **Phase 11/12 선행 작업 완료(2026-06-03)** — 프롬프트 품질 조율, golden OCR fixture/unittest, `originalText` OCR 포함 검증, `/localize-analysis` endpoint, 결과 화면 언어 key 누락 감지, session/history 병합 캐시 구현 완료. 분석 로딩 UX도 완료 전 3칸·완료 후 마지막 칸 방식으로 개선했다.
5. **카메라 UX 개편(A, Phase 8 폴리시)** — ✅ 구현 완료: `2026-06-02_camera_capture_ux_spec.md` / `2026-06-02_camera_capture_ux_plan.md`. `select.tsx` 단일 파일에 촬영↔검토 2모드, 검토 캐러셀, 카메라 `active=focused&&capture`, 갤러리 선택 후 검토 모드 전환 적용. 실기기에서 카메라 초록 점 해제만 수동 확인 필요.
6. **계약서 유형 분리 기획 추가(2026-06-05)** — 표준근로계약서를 `제조·건설·서비스`와 `농축산·어업` 2종으로 나누고, 설명 언어는 기존 16개를 유지하는 방식으로 확정했다. 네팔 PDF는 네팔어 전용 학습 데이터가 아니라 대표 양식 샘플이다. 기획: `2026-06-05_계약서유형_다국어프롬프트_기획.md`
7. **다음 세션 첫 작업** — `contractType` 선택 UI/API/프롬프트 분기 코드는 구현 완료했다. 실기기 Expo Go에서 두 계약서 유형을 각각 선택해 서버 로그의 `contractType` 값과 기존 16개 `language` 흐름 분리를 확인한다. 이어 실제 계약서 사진으로 `사진에서 확인` 하이라이트의 페이지·박스 위치·저장 재열람을 확인한다.

시작 전 `progress.json` 의 `nextActions` 와 PRD `F-17`~`F-19` 를 확인. 각 Phase 는 설계 합의(brainstorming) → 구현 순서로 진행한다.

## Phase 0. 기준 고정

목표: 개발 중 흔들리지 않게 범위와 샘플을 고정한다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| 대상 사용자 고정 | 외국인 근로자 방향으로 PRD와 AGENTS 정리 | done |
| 샘플 계약서 확보 | 표준근로계약서 또는 데모용 샘플 1장 선정 | todo |
| 정답 JSON 작성 | 사람이 직접 핵심 조건과 주의 조항 샘플 응답 작성 | todo |
| OCR API 선택 | CLOVA OCR 또는 Google Vision 중 하나로 결정 | todo |

## Phase 1. 앱 기본 골격

목표: Expo 앱에서 화면 이동과 이미지 선택까지 동작한다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| Expo + TypeScript 프로젝트 생성 | Android 기기 또는 에뮬레이터에서 첫 화면 실행 | todo |
| 홈 화면 | 분석 시작 버튼 표시 | todo |
| 이미지 선택 화면 | 갤러리 이미지 선택과 미리보기 동작 | todo |
| 분석 중 화면 | 업로드/OCR/AI 단계별 상태 UI 표시 | todo |
| 결과 화면 더미 UI | 샘플 JSON을 카드로 표시 | todo |

## Phase 2. FastAPI 서버

목표: 앱에서 서버로 이미지를 보내고 서버가 응답한다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| FastAPI 프로젝트 생성 | `/health` 응답 | todo |
| `/analyze-contract` 엔드포인트 | multipart 이미지 수신 | todo |
| 앱-서버 연결 | 앱에서 이미지 업로드 성공 | todo |
| 환경변수 설정 | OCR/OpenAI API 키를 서버에서만 사용 | todo |

## Phase 3. OCR 연결

목표: 계약서 이미지에서 실제 텍스트를 추출한다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| OCR API 클라이언트 작성 | 샘플 이미지 OCR 결과 반환 | todo |
| OCR 결과 정리 | 빈 줄/깨진 문자 최소 정리 | todo |
| OCR 실패 처리 | 글자 없음/네트워크 오류 안내 | todo |

## Phase 4. AI 분석 연결

목표: OCR 텍스트를 구조화된 분석 결과로 바꾼다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| OpenAI 프롬프트 작성 | 지정 JSON 형식으로 응답 | todo |
| 응답 스키마 검증 | 필드 누락/깨진 JSON을 잡아낸다 | todo |
| 핵심 조건 추출 | 임금, 근로시간, 휴일, 계약기간, 공제 표시 | todo |
| 주의 조항 생성 | 주의 레벨, 원문, 쉬운 설명, 번역 표시 | todo |
| 안전 문구 삽입 | 법률 자문 아님 안내 포함 | todo |

## Phase 5. 데모 안정화

목표: 발표 당일 API 장애가 있어도 핵심 흐름이 깨지지 않는다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| 샘플 모드 | 샘플 이미지/샘플 JSON으로 즉시 결과 표시 | todo |
| 로딩/오류 화면 | OCR 실패, AI 실패, 서버 오류 처리 | todo |
| 다국어 탭 | 한국어/영어/베트남어 전환 | todo |
| 결과 UI 정리 | 카드가 모바일 화면에서 읽기 좋음 | todo |

## Phase 6. 제출 준비

목표: 심사자가 실제 동작과 아이디어를 빠르게 이해한다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| Android 빌드 | APK 또는 개발 빌드 준비 | todo |
| 데모 영상 | 1분 이내 흐름 녹화 | todo |
| 발표자료 | 문제, 해결, 기술 구조, 차별점, 기대효과 정리 | todo |
| 신청서 초안 | Part 1/2/3 구조에 맞춰 작성 | todo |

## 줄이는 순서

시간이 부족하면 아래 순서로 줄인다.

1. 카메라 촬영을 빼고 갤러리 이미지 선택만 남긴다.
2. 베트남어 설명을 후순위로 두고 한국어+영어부터 완성한다.
3. APK 빌드를 뒤로 미루고 Expo 실행 영상으로 보완한다.
4. 실제 OCR 장애 대비로 샘플 모드를 발표 hero 흐름으로 사용한다.
5. AI 분석이 불안정하면 샘플 계약서에 대한 고정 JSON으로 데모를 완주한다.

## Phase 7. 앱 UI 리디자인 (done · 2026-06-01)

목표: "앱 같지 않다"를 해결한다. Toss 톤 + 인앱 카메라.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| 디자인 토큰 | 색/간격/라운드/그림자 한곳(`src/constants/theme.ts`) | done |
| SafeArea 적용 | 노치 대응(SafeAreaProvider) | done |
| 홈/로딩/결과 리디자인 | 이모지 제거, Toss 카드 스타일 | done |
| 인앱 카메라 스캐너 | expo-camera A4 뷰파인더 + 가이드 + 촬영/미리보기 | done |
| 색상 팔레트 최종 확정 | 토스블루 기준에서 브랜드 색 확정 | todo |

상세 결정: `2026-06-01_UI리디자인_결정.md`

## Phase 8. 분석 결과 저장·재열람 (+ 멀티페이지) (구현 done · 2026-06-02)

목표: 1회성 탈피. 여러 장 계약서를 한 번에 분석하고, 결과를 기기에 저장해 다시 본다. **서버 저장 X, 로컬만 · 계정 없음**(PRD §9 유지). 설계: `2026-06-01_phase8_저장재열람_spec.md` / 구현계획: `2026-06-01_phase8_저장재열람_plan.md`. 카메라 UX 개편(A) 설계: `2026-06-02_camera_capture_ux_spec.md` / 구현계획: `2026-06-02_camera_capture_ux_plan.md`. **구현 완료(tsc+iOS 번들 통과), SDK 54 Expo Go 호환 전환 완료, 수동 테스트 대기.**

저장 방식 확정: AsyncStorage(기록 JSON) + expo-file-system(이미지 N장).

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| 멀티페이지 캡처 | 촬영/갤러리로 여러 장 추가·삭제, 촬영↔검토 2모드, 검토 캐러셀, 갤러리 선택 후 검토 모드, 카메라 active 게이팅 | done |
| 멀티이미지 분석 | 앱이 N장 업로드 → 서버 장별 OCR 합쳐 분석 | done |
| 로컬 저장 | 분석 직후 결과 + 이미지 N장 + 시각 저장(샘플 폴백은 '샘플' 표시) | done |
| 내역 목록 화면 | 썸네일·N장·날짜·요약·주의건수, "이 기기에만 저장" 안내 | done |
| 상세 재열람 | 목록 항목 눌러 결과 재사용 + 계약서 원본 N장 보기 | done |
| 삭제 | 개별 스와이프 삭제 + 목록 선택 삭제 모드(항목 체크, 전체 선택, 선택 삭제, 취소/완료 해제). 상세 페이지 삭제는 기존 흐름 유지 | done |
| 흐름 UX | 멀티페이지 로딩 문구, 저장됨 안내, 결과 후 홈/목록 분기 | done |

후순위(선택): 핀치 줌, 이미지 다운스케일(저장 용량 커지면).

## Phase 9. 다국어 확장 + 마이페이지(언어 설정) (구현 done · 2026-06-02)

목표: 라벨만이 아니라 **값까지** 사용자가 고른 언어로. 국적/국가를 묻지 않고 언어 토글/리스트로 앱 전체 UI와 분석 결과 언어를 함께 설정.

지원 언어: 한국어 · English · Nepali · Tetum · Russian · Mongolian · Burmese · Bengali · Vietnamese · Uzbek · Indonesian · Chinese · Khmer · Kyrgyz · Thai · Lao.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| 마이페이지 | 언어 설정 UI, 선택값 로컬 저장 | done |
| 언어 모델 확장 | 앱 UI i18n + 14개 대상 언어 결과 데이터 구조 | done |
| 값 번역 방식 결정 | AI가 ko/en/요청언어 결과를 생성. 원문은 한국어 유지 | done |
| 백엔드 연동 | `language` 파라미터를 16개 언어로 확장 | done |
| 앱 전역 적용 | 홈/촬영/검토/로딩/결과/기록/삭제/오류 문구가 선택 언어로 표시 | done |
| 결과 화면 적용 | 요약 값·주의 조항 내용이 선택 언어로 표시 | done |

결정: 선택 언어는 앱 전체 UI와 분석 결과에 함께 적용한다. 번역 품질·토큰 비용은 대표 언어 몇 개로 흐름 검수하고, 전체 언어는 키 누락/폴백/렌더링 깨짐을 점검한다. (PRD F-14·F-15)

남은 수동 검증: Expo Go 실기기에서 언어 설정을 여러 언어로 바꾸며 홈·촬영·로딩·결과·기록·설정 문구의 줄바꿈과 버튼 폭을 확인한다.

## Phase 10. 사진 출처 하이라이트 (후속)

목표: 주의 조항이 계약서 사진의 **어디에서 나온 것인지** 사용자가 직접 확인할 수 있게 한다. 결과를 "믿으라"가 아니라 "원본에서 확인하라"는 신뢰 UX로 만든다. 설계 메모: `2026-06-02_phase10_사진출처_하이라이트_설계메모.md` (PRD F-17)

2026-06-03 조정: 사진 하이라이트 구현 전에 `2026-06-03_다음기획_프롬프트_UIUX_출처하이라이트_다국어.md` 의 Phase 10A~10C를 먼저 진행한다. 하이라이트 정확도는 AI의 `originalText` 품질과 결과 화면 동적 다국어 구조에 의존한다.

2026-06-04 1차 구현: 서버가 Google Vision OCR 줄 영역을 정규화 좌표로 보존하고, `originalText`를 OCR 영역에 exact/normalized 순서로 매칭해 `cautionItems[].source`를 붙인다. 앱 결과 카드에는 `사진에서 확인`/매칭 실패 안내를 표시하고, 원본 이미지 뷰어에는 contain 여백 보정 하이라이트와 OCR 원문 근거 패널을 보여준다. 남은 일은 실기기/실 OCR 사진에서 박스 위치 QA다.

2026-06-05 보강: 문자열 매칭만으로는 AI가 재구성한 `originalText`와 OCR 원문이 어긋날 수 있어, OCR 줄마다 `p1-r4` 같은 ID를 붙이고 AI가 `sourceRegionIds`를 반환하도록 바꿨다. 이 기능은 "AI 결과를 믿으라"가 아니라 "사진 속 원문에서 직접 확인하라"는 신뢰 UX이며, 공모전 어필 포인트는 `2026-06-05_OCR출처확인_UX_어필포인트.md`에 정리했다.

2026-06-06 표시 보강: OCR 좌표 자체는 유지하되 모바일 뷰어에서만 하이라이트 rect에 여백을 더하고, 연한 채움+전체 테두리로 바꿨다. 목적은 위치 정확도를 과장하지 않으면서 OCR 글자를 가리지 않는 것이다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| OCR 좌표 보존 | Google Vision 응답에서 페이지별 word/block bounding box를 보존하고 정규화 좌표로 변환 | done |
| 조항↔사진 위치 매칭 | AI가 반환한 `originalText/sourceQuote` 를 OCR 단어/문장 좌표와 exact/normalized/fuzzy 순서로 매칭 | done |
| 결과 카드 진입 | 매칭된 주의 카드에 `사진에서 확인` 액션을 표시하고, 실패 시 `원문 위치를 정확히 찾기 어려워요` 안내 | done |
| 이미지 뷰어 하이라이트 | 원본 이미지 해당 페이지에 반투명 박스를 오버레이하고 하단에 카드 제목·원문 인용 표시 | done |
| 저장 모델 확장 | 새 분석 기록에만 `source` 좌표와 confidence 저장. 기존 기록 소급 적용 없음 | done |
| 실기기/실 OCR QA | 실제 촬영 계약서에서 하이라이트 페이지·위치·저장 재열람을 확인 | todo |

중요 원칙: AI가 좌표를 직접 만들지 않는다. 좌표는 OCR 결과와 서버 매칭 로직이 계산한다. 매칭 신뢰도가 낮으면 억지로 하이라이트하지 않고 원문 인용만 제공한다.

## Phase 11. 프롬프트 품질 조율 (done · 2026-06-03)

목표: 실제 OCR 텍스트에서 빈 양식, 비정상 값, 공제, 위약금, 여권 보관, 수당 누락을 안정적으로 구분한다. `originalText`는 Phase 10 사진 하이라이트의 매칭 기준이므로 OCR 원문과 실제로 매칭 가능한 짧은 한국어 인용이어야 한다. 상세 계획: `2026-06-03_다음기획_프롬프트_UIUX_출처하이라이트_다국어.md`

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| 프롬프트 구조 정리 | 역할·출력형식·체크리스트·추출규칙·다국어 규칙이 분리됨 | done |
| golden OCR 케이스 작성 | 빈 양식/비정상 임금/비정상 시간/공제/위약금/여권 보관/수당 누락 검증 가능 | done |
| originalText 품질 기준 | OCR 원문에 실제 존재하고 서버 매칭 가능한 짧은 인용으로 제한 | done |
| 서버 검증 | `py_compile`, real AI JSON schema 검증, 502 오류 처리 유지 | done |

## Phase 12. 동적 다국어 보강 + 결과 UI/UX 정비 (부분 done · 2026-06-03)

목표: 결과 화면 상단에서 언어를 바꿨을 때 카테고리/UI 라벨뿐 아니라 AI가 생성한 요약값·주의 조항 설명도 해당 언어로 보이게 한다. 현재는 분석 당시 생성되지 않은 언어 key가 없어서 영어 fallback이 발생한다.

권장 방식: 분석 시 16개 언어를 모두 생성하지 않고, 결과 화면에서 선택한 언어가 없을 때만 서버에 보강 요청을 보내고 session/history record에 병합한다.

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| 언어 key 누락 감지 | 결과 화면이 선택 언어의 동적 필드 존재 여부를 판단 | done |
| 보강 API 설계 | 기존 분석 결과에서 target language만 생성하는 endpoint 추가 | done |
| 앱 병합/캐시 | session과 저장 기록에 새 localized fields 병합 | done |
| UI 상태 | 번역 로딩/실패/fallback 상태가 명확히 보임 | done |
| 결과 화면 정비 | 언어 전환, 요약, 주의 카드, 사진에서 확인 진입이 자연스럽게 정렬 | done |

추가 완료(2026-06-03): 분석 로딩 진행 바는 완료 전 3칸까지만 천천히 채우고, 실제 분석 완료 후 마지막 칸을 채운 뒤 결과 화면으로 이동한다.

추가 완료(2026-06-04): 내역 목록 우측 상단 휴지통은 즉시 전체삭제가 아니라 선택 삭제 모드로 진입한다. 선택 모드에서 항목 체크, 전체 선택, 선택 삭제, 취소/완료 해제를 제공한다. 상세 페이지 삭제 UX와 사진 출처 하이라이트는 변경하지 않았다.

추가 완료(2026-06-04): 결과 카드에서 OCR 원문 근거를 사진 위치와 연결했다. `사진에서 확인`을 누르면 해당 페이지 원본 이미지에 OCR 영역 하이라이트를 보여주고, 하단 패널에 OCR 원문과 분석 근거 안내를 표시한다.

## Phase 13. 계약서 유형 선택 + 업종별 프롬프트 분기 (구현 done · 실기기 QA 대기 · 2026-06-06)

목표: 표준근로계약서 양식 차이를 반영한다. 사용자가 분석 전 `제조·건설·서비스` 또는 `농축산·어업`을 선택하면, 서버 AI가 해당 양식의 체크리스트를 적용한다. 설명 언어는 기존 16개를 그대로 유지한다. 상세 기획: `2026-06-05_계약서유형_다국어프롬프트_기획.md` / 구현 계획: `2026-06-06_계약서유형_구현계획.md` (PRD F-20)

| 작업 | 완료 기준 | 상태 |
|---|---|---|
| 계약서 유형 상수 | `manufacturing_construction_service`, `agriculture_livestock_fishery` 두 값으로 모바일/서버 기준 통일 | done |
| 분석 전 선택 UI | `/select` 검토 화면의 분석 버튼 위에서 두 유형 중 하나를 선택 | done |
| API payload 확장 | `POST /analyze-contract` multipart에 `contractType` 추가. 기존 `language` 16개 흐름 유지 | done |
| 서버 수신/검증 | FastAPI가 `contractType`을 수신하고 허용값이 아니면 기본값으로 보정 | done |
| 프롬프트 분기 | 제조·건설·서비스는 교대제/연장·야간·휴일수당/임금 세부 항목, 농축산·어업은 농번기·성어기/업무 세부기재/휴일 조정/숙식비를 더 강하게 확인 | done |
| 제목 mismatch 안내 | OCR 제목과 사용자가 고른 유형이 어긋나면 법률 판정이 아니라 "선택한 종류와 다를 수 있음"으로 안내 | done |
| 테스트 | 프롬프트에 유형별 기준 문구가 포함되고 기존 다국어 validation이 깨지지 않음 | done |

원칙:

- 네팔 PDF는 대표 샘플이며 네팔어 전용 분석으로 좁히지 않는다.
- `contractType`은 계약서 양식/업종 축이고, `language`는 설명 언어 축이다.
- 농축산·어업의 근로시간·휴일은 일반 양식 기준으로 단정 경고하지 않는다.
- 1차 구현은 응답 스키마를 바꾸지 않고 프롬프트 컨텍스트로만 사용한다.

구현 순서:

1. 서버 프롬프트 분기 테스트 추가
2. 서버 `contractType` 정규화와 유형별 체크리스트 블록 추가
3. FastAPI `contractType` form field 수신
4. 모바일 세션/API payload 연결
5. `/select` 검토 화면 분석 버튼 위 선택 UI 추가
6. 서버 unittest, 모바일 `tsc`, 실기기 QA
